.container
  .create-wrapper
    = form_with(model: @comment, url: forum_comments_path(@comment.forum), local: true) do |f|
      
      %div
        = f.label :content, 'コメントを追加する'
      %div
        = f.text_area :content, class: 'text'
      = f.submit '保存', class: 'btn-primary'
